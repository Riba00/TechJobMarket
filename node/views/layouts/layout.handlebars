<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css?family=Rubik:400,700,900" rel="stylesheet">
    <link rel="stylesheet" href="/css/trix.css">
    <link rel="stylesheet" href="/css/app.css">
    <title>{{pageName}}</title>
</head>
<body>
    <div class="nombre-sitio contenedor">
        <h1>
            <a href="/">TechJobMarket</a>
        </h1>

        {{#if bar}}
            <div class="buscador">
                <form action="#">
                    <input type="text" class="buscar">
                    <input type="submit" value="Search">
                </form>
            </div>
        {{/if}}

        {{#if logOut}}
        <div class="cerrar-sesion">
            <p>Hello! {{name}}</p>
            {{#if photo}}
                <img src="/uploads/profiles/{{photo}}" alt="Profile Photo" class="admin-perfil">
            {{/if}}
            <a href="/administration">Administration</a>
            <a href="/logOut">Log Out</a>
        </div>


        {{/if}}
    </div>

    <header class="site-header contenedor separador">
        <h2>{{pageName}}</h2>

        {{#if tagLine}}
            <p class="tagline">{{ tagLine }}</p>
        {{/if}}

        {{#if button}}
            <a href="vacancies/new" class="btn btn-azul">Publish New Vacancy</a>
        {{/if}}
    </header>

    <div class="contenido-principal contenedor">

        {{#if messages }}
        <div class="alertas">
            {{#showAlerts messages}}
            {{/showAlerts}}
        </div>

        {{/if}}

        {{{body}}}
    </div>
    
    <script src="/js/trix.js"></script>
    <script src="/dist/bundle.js"></script>
</body>
</html>